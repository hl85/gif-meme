
## 语言
所有回复必须使用简体中文。

## 部署
需要部署时，先读取 @deploy skill，再执行任何操作。

## 设计
涉及 UI、视觉、布局、组件样式、主题、交互文案时，先读取 @design skill，再开始设计或实现。
`@design` 会要求读取 `./rules/design.md` 作为视觉基线。
不要凭空发明一套新视觉语言；优先复用现有 token、组件模式和页面层级约束。
